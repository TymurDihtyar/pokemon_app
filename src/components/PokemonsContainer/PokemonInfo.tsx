import {FC, PropsWithChildren} from 'react';

interface IProps extends PropsWithChildren {

}

const PokemonInfo:FC<IProps> = ({}) => {

    return (
        <div>

        </div>
    );
};

export {PokemonInfo};